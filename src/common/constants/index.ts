const DATE_FORMAT = 'YYYY-MM-DD HH:mm:ss';
const HEADERS = {
  ContentType: 'text/html',
};
const PER_PAGE = 10;
const PAGE = 12;

const DIR_PATH = `${process.cwd()}/src`

export default {
  DIR_PATH,
  DATE_FORMAT,
  HEADERS,
  PER_PAGE,
  PAGE,
};
